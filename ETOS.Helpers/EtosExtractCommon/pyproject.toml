[project]
name = "etos-extract-common"
version = "0.1.0"
description = "Shared XML-driven SQL extraction and export utilities for ETOS helper projects"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["etos_extract_common"]
